Projeto Retângulo

-🎯Objetivo: seguindo o diagrama fornecido da classe Retangulo, implementar o 
código e, em seguida, criar duas outras classes para de execução (AppQuestao2Ui e AppQuestao3Ui).
E, por último, fazer uma interface gráfica que permita que o usuário informe as medidas do retângulo,
limpe os campos e realize os cálculos sobre a forma geométrica.

-🛠️Tecnologias: linguagem java, biblioteca JavaSwing, NetBeans IDE. 

-📂Estrutura do projeto: estrutura em camadas, separando as partes que possuem interface gráfica (pacote View) e as partes que não (pacote Model).
